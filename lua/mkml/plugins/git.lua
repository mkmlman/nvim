return {
  'dinhhuy258/git.nvim',
  -- this is currently not needed, as i can live with using :!git instead of :Git for now. once i'm fuming with no color highlights, then can enable this. 
  -- this plugin is a wrapper on vim-fugitive for neovim. 
--  config = function()
  --  require('git').setup()
    --end
}
